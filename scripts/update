#!/bin/bash
# Pull latest code and rebuild/restart the container
set -e

cd "$(dirname "$0")/.."

echo "Pulling latest changes..."
git pull --ff-only

echo "Rebuilding and restarting..."
docker compose up --build -d

echo "Done! Check status with: docker compose logs -f"
